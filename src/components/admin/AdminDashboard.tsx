import * as React from 'react';
import { HMSSettingsPage } from './HMSSettingsPage';
import { useAuth } from '../../hooks/useAuth';

export const AdminDashboard = () => {
  return <HMSSettingsPage />;
};